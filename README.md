# ai-guide-portal

> This file is generated by **automatic instantiation** of meta repository `openstrata-meta/template/app-skeleton` and is a member of the OpenStrata multi-repository (polyrepo) system. The structure follows Architecture Document v2.8 §15.6.2.

- **Language**: TypeScript (Primary framework: React 18 + Vite + Ant Design (front-end) / Spring Boot 3.x (portal back-end))
- **domain**: assembly
- **OPTIONAL**: core
- **Meta repository reference**: `openstrata-meta/repos.yaml` (tag `v1.0.0`) · BOM see `openstrata-meta/bom.yaml`

## Repository unified structure (§15.6.2)

```
ai-guide-portal/
├── src/ or cmd/            #Application code (according to §15.5.2 DDD four layers)
├── infrastructure/config/  #★ This repository SPI adapter local configuration fragment
├── Dockerfile / helm/      #Deployment artifacts
├── .github/                #Independent CI for each repository (build/test/scan/publish)
├── arch/                   #★ Architectural positioning (the role/boundary of this repository in the layering)
├── design/                 #★ Design rules + ADR (evolutionary AI coding guidelines)
├── skills/                 #★ AI coding skills (for consumption by CodeBuddy/Cursor, etc.)
└── specs/                  #★ Specifications and contracts (API/AgentSpec/SPI Schema)
```

## Responsibilities of this repository (TODO: completion)

Describe in 1-3 sentences: the role of this repository in the layered architecture, the **SPI ports** exposed/dependent, and the external open source components it relies on (default ✅/optional).

## Local development (TODO: completion)

- Build/Test/Run commands
- How to access meta repository `dependencies/` dependency graph and `profiles/` presets

> Evolutionary AI coding: The `docs/ (ARCH.md, DESIGN.md, SKILLS.md, SPECS.md, adr/)` of this repository is the source of truth shared by AI assistants and contributors; new decisions are recorded as ADRs in `docs/adr/`.

## Codegen deliverable (2026-07-18, Phase 1)

Generated by the OpenStrata parallel codegen harness (CodeBuddy backend) per
`prompts/ai-guide-portal.md` + `docs/DESIGN.md`. Implements the bounded modules:

- **capability-catalog** — `src/domain/capabilityCatalog.ts` is the SINGLE
  SOURCE OF TRUTH (ADR-0001), rendered by `src/components/CapabilityCatalog.tsx`
  as a card grid + `DataTable`. Runtime shape matches the backend
  `GET /api/v1/capabilities`.
- **preview-calc** — `src/application/preview/usePreviewCalc.ts` computes the
  reuse-M caliber (ADR-0002): a component is REUSED when already enabled in the
  tenant manifest, else ADDed.
- **rollback-ux** — `src/application/rollback/useRollback.ts` + `RollbackUx`
  offers BOTH manifest-level and per-component declarative rollback (ADR-0003).
- **grayscale-upgrade-display** — `src/application/canary/useCanaryDisplay.ts`
  + `CanaryDisplay` shows the dual-write → verifying → cutover phases via
  ai-ui-kit `MermaidRenderer` (ADR-0004).
- **go-engine-contract-client** — `src/domain/goEngineContract.ts` +
  `GoEngineContractClient` pins the `bom.yaml` `interface_versions` (R-004) as
  the single SemVer source of truth; never diverges.

Plus `GuideApiClient` (X-Tenant-Id + Bearer, DESIGN §5), `GuidePort` (dependency
inversion), `SessionProvider` (dev mints `tenant_id=local`, ADR-0002 analogue),
`OsProvider` theme bridge, react-router routing, Zustand store. Reuses
ai-ui-kit `DataTable`/`TenantSwitcher`/`MermaidRenderer`/`OsProvider`.

Build/run:
```bash
npm install
npm run build      # tsc && vite build -> dist/
npm run dev        # vite dev on :5173
```
Verify gate (`npm run build`) **PASS**.

> Local dev note: `@openstrata/ai-ui-kit` is resolved to the sibling source via
> vite alias + tsconfig paths (not yet published). Remove the alias and add the
> published package before production use.
